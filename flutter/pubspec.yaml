name: scrywatch
description: Official Flutter/Dart SDK for ScryWatch log monitoring and session tracking.
version: 1.1.0
homepage: https://scrywatch.com/guides/flutter-sdk
repository: https://github.com/scrywatch/sdk-flutter

topics:
  - logging
  - monitoring
  - observability

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
